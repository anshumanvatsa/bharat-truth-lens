# Add these to your existing requirements.txt
# Run: pip install -r requirements_improvements.txt

# SpaCy NER (Step 1)
spacy>=3.7.0
# After pip install, also run:
# python -m spacy download en_core_web_lg

# Bootstrap CI (Step 2)
scipy>=1.11.0
scikit-learn>=1.4.0
numpy>=1.24.0

# BERTScore (Step 3)
bert-score>=0.3.13
# Requires transformers and torch (likely already installed)

# NLI Stance Detection (Step 4)
transformers>=4.40.0
torch>=2.0.0
# Model will auto-download on first run (~900MB):
# cross-encoder/nli-deberta-v3-base

# Already in your requirements (no change needed):
# sentence-transformers
# llama-cpp-python